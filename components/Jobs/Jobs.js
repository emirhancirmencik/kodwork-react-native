import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Jobs = () => {
  return (
    <View>
      <Text>Test</Text>
    </View>
  );
};

export default Jobs;

const styles = StyleSheet.create({});
